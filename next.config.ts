import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: [
      'picsum.photos',
      'images.unsplash.com',
      'i.pinimg.com',
      'i.pravatar.cc',
      'res.cloudinary.com',
      'images.unsplash.com'
    ]
  }
}

export default nextConfig
